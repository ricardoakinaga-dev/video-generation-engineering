# Triple-AAA Closure R4 — master prompt production closure

<!-- engineering-framework: active_action_id=VGE-TRIPLE-AAA-R4:FRESH-CRITIC -->

## Purpose

Implement and audit the complete user-supplied `MASTER PROMPT — FINAL TRIPLE-AAA PRODUCTION CLOSURE` against the existing brownfield video-generation-engineering Skill. Preserve R1/R2/R3 history, do not rewrite prior evidence, and never promote mechanical or structural evidence to audiovisual production proof.

## Source and frozen bar

- User source: `/home/ricardo/.codex/attachments/4faa850c-b83b-43b9-8476-25ee472aef1a/pasted-text-1.txt`
- Repository copy: `docs/master-prompt-triple-aaa-final-production-closure-20260909.txt`
- Source SHA-256: `7142fab73b44805a4c03eaeba67824c35519546cf3b801ee51c0976a8390fc99`
- Repository copy SHA-256: `8f5e6ff6521bf941d5f811e9d3401b7ac0893b2497c23db3510a5daefadf6472`
- Source bytes/lines: `23649 / 1725`; repository bytes/lines: `23650 / 1726`
- Normalization: content preserved; one final LF was added by the patch transport.
- Frozen bar: `docs/triple-aaa-quality-bar-r4.json`

## Non-negotiable quality bar

1. Baseline current code, profiles, tests, docs, artifacts, runtime, queue, GPU/VRAM, models, workflow hashes, distribution and critic state before material work.
2. Keep intent, canonical state, prompt views, runtime attempts, bytes and observations as separate linked truth layers.
3. Use only allowed capability states in the current matrix: `PROVEN`, `PARTIAL`, `UNKNOWN`, `BLOCKED`, `NOT_RUN`, `NOT_APPLICABLE`.
4. Run the full regression gate: tests, compile, Skill validation, docs/schema, manifest, external-CWD smoke, import-cycle check, secret/absolute-path/weight/media-privacy scans.
5. Use a fresh selected-device/free-VRAM guard before every real submission; do not cancel or supersede unrelated work.
6. Attempt LF-001 first, then its real repair/re-anchor and assembly, then FLF, LF-002, LF-003, LF-004 only when dependencies and resources permit; preserve all failures.
7. Require real artifact hashes, media QA, semantic/continuity/audio/transition/editorial oracles and bounded repair lineage. A generated file is not automatically accepted.
8. Use the prompt compiler, contradiction gate, risk-based negative constraints, canonical-state bindings and immutable attempt/evidence contracts.
9. Obtain a final fresh non-inherited critic after material changes. The critic owns its scope fingerprint, timestamps, criteria matrix and mutation sentinel; any post-critic mutation invalidates it.
10. Freeze distribution only after the final critic; record integrity, manifest, scans, compile, Skill validation and external-CWD behavior.

## Concrete Steps

1. [VGE-TRIPLE-AAA-R4:FRESH-CRITIC] — obtain a fresh non-inherited read-only review of the post-cross-shot-gate candidate, with reviewer-owned fingerprint and mutation sentinel; preserve the current PARTIAL production boundary. **IN PROGRESS.**

2. [VGE-TRIPLE-AAA-R4:BASELINE] — freeze the R4 bar, reconcile the stale R3 control-plane pointer, run fresh regression/security/portability checks and capture the current ComfyUI/resource observation before any new POST. **COMPLETE.**

3. Revalidate the local H3 profile, node inventory, model assets, workflow and device/resource context. **COMPLETE for the exact H3 T2V scope.**

4. Attempt bounded real LF-001 shots, collect immutable artifacts and run mechanical, semantic, continuity and transition gates. **COMPLETE with S01/S02 accepted, S03 partial and T02 failed.**

5. Attempt only evidence-authorized repair/re-anchor, FLF and later ladders; preserve every failure and stop on resource/capability blockers. **COMPLETE as a disposition: repair is awaiting authorization; FLF/LF-002/LF-003/second adapter remain NOT_RUN/BLOCKED.**

6. Update the R4 matrix, scorecard and final closure report, obtain the fresh critic, freeze distribution, commit and push. **The post-hardening critic and final remote handoff remain open.**

## Execution order

- [x] Preserve the new master prompt and record source/repository hashes.
- [x] Freeze R4 bar and append recovery/current-task control-plane records.
- [x] Run fresh regression and security/portability baseline.
- [x] Revalidate ComfyUI profile, node inventory, model assets, workflow and device/resource context.
- [x] Attempt bounded real LF-001 shots on the currently supported local device; collect, inspect and record semantic/transition status.
- [x] Attempt only evidence-authorized repair/re-anchor, FLF and later ladders; do not fabricate a failure to demonstrate repair.
- [x] Update R4 matrix, scorecard and closure report with exact status and unsupported claims.
- [ ] Run fresh final critic with before/after fingerprint and mutation sentinel.
- [x] Build final distribution and rerun the release gate.
- [x] Commit, push and verify the remote tip.

## Recovery rules

- Old `.agent/` and `.gauntlet/` records are append-only history. R3 remains blocked/incomplete where it was.
- A queue timeout is `UNKNOWN`; inspect the same queue/history ID before any further submission.
- OOM, failed, partial, stale-profile and collection errors remain immutable evidence.
- Resource observations are scheduling evidence, not an inference guarantee.
- If LF-001 cannot reach acceptance, the final verdict is `READY_WITH_RISKS`, `PARTIAL` or `BLOCKED`, never `TRIPLE_AAA_PROVEN`.

## Completion signal

The repository contains the saved prompt, R4 frozen bar, updated Skill/docs/tests/evidence, an honest final report with the exact requested headings, an independent fresh critic record, a frozen distribution and a verified commit pushed to `origin/main`. Production claims are limited to artifacts and oracles actually observed.

## Final handoff

The R4 closure packet was committed and pushed to the configured `origin/main`. The remote tip was verified against the local tip after the push. A subsequent evidence audit found that transition acceptance lacked an explicit cross-shot comparison gate, so the package was reopened for a bounded contract hardening pass; the previous critic is historical for the pre-hardening candidate.

## Post-critic hardening continuation — 2026-09-09

- [x] Add `validate_cross_shot_comparison` with exact previous/next artifact bindings and all 14 continuity dimensions.
- [x] Make `validate_transition_contract` fail closed when the comparison is missing, and keep `NOT_OBSERVED` comparisons below acceptance.
- [x] Reconcile R4 T01/T02 evidence and documentation; T01 is now `PARTIAL`, T02 remains `FAIL`.
- [x] Re-run focused/full tests, compileall, offline verification and current documentation audit.
- [x] Rebuild the portable package as R7; obtain a new fresh non-inherited critic before final remote handoff.

Current action: `VGE-TRIPLE-AAA-R4:FRESH-CRITIC`. The audiovisual verdict remains `PARTIAL`; this hardening closes a software acceptance gap but does not manufacture missing side-by-side media observations or the remaining LF/FLF/adapter evidence.

## Purpose / Big Picture

Close the supplied R4 master prompt against the brownfield Skill while preserving a strict separation between deterministic software proof and audiovisual production proof. The current milestone is a fresh review of the post-hardening candidate.

## Progress

- [x] (2026-09-09T21:50:00Z) Implemented and tested the explicit hash-bound cross-shot comparison gate; R4 T01 is `PARTIAL`, T02 is `FAIL`.
- [ ] (2026-09-09T22:02:00Z) Complete the fresh non-inherited critic and record its fingerprint/sentinel.

## Surprises & Discoveries

The prior transition contract could accept state equality plus a next-shot scorecard without any explicit comparison between the two artifacts. The local runtime evidence still lacks the visual/audio oracle needed to promote R4 transitions.

## Decision Log

- Keep the comparison contract mandatory and fail closed; do not infer cross-shot identity from metadata, state equality or per-shot contact sheets.
- Preserve the original critic memo as historical evidence for its pre-hardening snapshot; commission a new critic after this material change.
- Do not run an expensive repair, provider call or model download without the required capability/authorization boundary.

## Outcomes & Retrospective

Software quality improved from an over-permissive transition gate to explicit 14-dimension comparison accounting. The production verdict remains `PARTIAL`; the gate hardening cannot substitute for missing runtime evidence.

## Context and Orientation

The executable Skill is under `.agents/skills/video-generation-engineering/`; quality contracts live in `vge_quality.py`, runtime provenance in `vge_runtime.py`/`vge_evidence.py`, and current R4 evidence under `verification/`. The frozen authority is `docs/triple-aaa-quality-bar-r4.json`.

## Scope and Constraints

In scope: transition contract hardening, tests, R4 evidence reconciliation, docs, package verification and fresh review. Out of scope without new authority: paid calls, uploads, foreign queue mutation, large model downloads and unsupported audiovisual claims.

## Architecture and Interfaces

`validate_transition_contract` binds previous/next artifacts and delegates adjacent visual/audio comparison to `validate_cross_shot_comparison`. The latter validates all `CONTINUITY_DIMENSIONS`, exact hashes and aggregate status before a transition can be `PASS`.

## Milestones

### M1 — Cross-shot contract and regression coverage

Complete.

### M2 — R4 evidence/docs/package reconciliation

Complete.

### M3 — Fresh critic and final remote handoff

Pending.

## Plan of Work

Run the fresh read-only critic against the current candidate, capture its criterion matrix and stable scope fingerprint, then update only reviewer-independent release accounting. If the critic passes software scope but rejects audiovisual production, preserve `READY_WITH_RISKS`/`PARTIAL` and push the honest result.

## Validation and Acceptance

Acceptance requires 143 tests, compileall, Skill/package validation, current docs audit, R7 distribution CRC/path/security scan, and a fresh non-inherited critic. Triple-AAA production additionally requires accepted LF-001/LF-002/LF-003, repair, FLF, second adapter and semantic/editorial evidence; those remain unmet.

## Risks and Human Decisions

The S03 repair plan remains awaiting authorization. Human review is required before any costly or capability-expanding runtime action; no reviewer verdict may promote unobserved audiovisual behavior.

## Idempotence and Recovery

Critic execution is read-only and may be repeated only against a newly fingerprinted candidate. Existing attempts, artifacts and reviewer memos are immutable; after any product mutation, invalidate the prior critic and rebuild the distribution.

## Artifacts and Evidence

Current evidence: `verification/software-triple-aaa-r33.json`, `verification/docs-current-r28.json`, `verification/distribution-triple-aaa-r7.json`, `verification/long-form/LF-001-T01-r4-transition.json`, `verification/long-form/LF-001-T02-r4-transition.json` and the forthcoming post-hardening critic memo.
