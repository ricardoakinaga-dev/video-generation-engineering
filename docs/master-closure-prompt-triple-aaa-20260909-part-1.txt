# MASTER CLOSURE PROMPT

# video-generation-engineering

# STATE-OF-THE-ART / TRIPLE-AAA FINAL PRODUCTION CLOSURE

Repository:

https://github.com/ricardoakinaga-dev/video-generation-engineering

Skill:

`video-generation-engineering`

Mission:

Take the CURRENT mature repository and close the remaining evidence, runtime, audiovisual, long-form, repair, adapter and release gaps required for the strongest defensible State-of-the-Art / Triple-AAA release.

This is NOT a greenfield build.

This is NOT permission for a rewrite.

This is NOT a request for more architecture for architecture's sake.

The current system is already strong in:

* canonical scene modeling;
* desired/planned/observed truth separation;
* Scene Bible;
* shot graph;
* continuity ledger;
* reference retention;
* interaction decomposition;
* dialogue/performance contracts;
* audio contracts;
* prompt compilation;
* model adaptation;
* ComfyUI runtime integration;
* resource guards;
* provenance;
* deterministic QA;
* semantic QA contracts;
* transition contracts;
* long-form fixtures;
* progressive disclosure;
* portability;
* regression testing.

The remaining problem is primarily:

PRODUCTION PROOF.

The target is to move from:

`READY_WITH_RISKS`

toward:

`TRIPLE_AAA_CANDIDATE`

or:

`TRIPLE_AAA_PROVEN`

ONLY IF THE EVIDENCE JUSTIFIES IT.

---

# 0. GOVERNING PRINCIPLE

The final system must distinguish:

WHAT THE USER WANTS

from:

WHAT THE SYSTEM PLANS

from:

WHAT THE SYSTEM EXECUTES

from:

WHAT THE MODEL ACTUALLY PRODUCES

from:

WHAT THE EVIDENCE ACTUALLY PROVES.

Never collapse these layers.

Never convert:

DOCUMENTATION
TEST PASS
SCHEMA VALIDITY
RUNTIME AVAILABILITY
NODE PRESENCE
GENERATED BYTES

into:

PRODUCTION QUALITY

without appropriate evidence.

---

# 1. NO BLIND REWRITE

Before changing anything:

AUDIT THE CURRENT REPOSITORY.

If an existing implementation already satisfies a requirement:

KEEP IT.

If a proposed change increases complexity without measurable production value:

REJECT IT.

If an existing abstraction has clear ownership and tests:

DO NOT replace it merely for stylistic reasons.

Use:

AUDIT
→ GAP
→ MINIMAL CHANGE
→ TEST
→ RUNTIME
→ OBSERVE
→ REPAIR
→ VERIFY

---

# 2. CURRENT BASELINE TO VERIFY

Do not trust these statements blindly; verify them.

Expected current baseline includes approximately:

* 137 passing tests;
* valid Skill package;
* portable distribution;
* H3/ComfyUI integration;
* runtime resource guard;
* H3 historical T2V evidence;
* H3 R2V partial evidence;
* LF-001 A001/A002 OOM evidence;
* LF-001 A003 S01 real artifact;
* LF-001 S01 mechanical PASS;
* LF-001 S01 semantic PARTIAL;
* LF-001 full production still BLOCKED;
* LF-002 BLOCKED;
* LF-003 BLOCKED;
* LF-004 NOT_RUN;
* FLF UNKNOWN/NOT_OBSERVED;
* second adapter not production proven;
* final frozen independent critic not closed.

Create a fresh baseline matrix before edits.

---

# 3. QUALITY TARGET

Triple-AAA consists of THREE independent pillars.

## AAA ARCHITECTURE

Must demonstrate:

* canonical model-independent scene representation;
* explicit state;
* explicit continuity;
* bounded responsibilities;
* progressive disclosure;
* feature-scoped adapters;
* maintainability;
* graceful degradation;
* truthful capability negotiation.

## AAA VERIFICATION

Must demonstrate:

* deterministic tests;
* known-bad fixtures;
* adversarial fixtures;
* metamorphic tests;
* integration tests;
* runtime probes;
* immutable attempts;
* artifact hashes;
* semantic observations;
* transition observations;
* repair evidence;
* assembly lineage;
* fresh independent critic.

## AAA PRODUCTION

Must demonstrate:

* real generated multi-shot media;
* accepted continuity;
* accepted interaction;
* accepted semantic observations;
* real targeted repair;
* final assembly;
* audio/video acceptance where applicable;
* long-form production evidence;
* human editorial checkpoint where required.

One pillar cannot compensate for another.

---

# 4. PRIORITY ORDER

Work in this exact priority order unless hard evidence requires a change:

P0 — regressions/security/evidence correctness

P1 — LF-001 complete production

P2 — real repair/re-anchor

P3 — FLF capability

P4 — LF-002 dialogue/audio/lip-sync

P5 — LF-003 45–60 second production

P6 — second real adapter

P7 — architecture/context minimization

P8 — fresh independent critic

P9 — final distribution

Do not start speculative new features while P1–P5 remain open.

---

# 5. PHASE A — FRESH BASELINE

Inspect:

* SKILL.md;
* references;
* scripts;
* profiles;
* templates;
* tests;
* docs;
* verification;
* artifacts;
* runtime state;
* queue;
* GPUs;
* VRAM;
* ComfyUI version;
* node inventory;
* workflows;
* workflow fingerprints;
* model files;
* model fingerprints;
* current critic records;
* current distribution.

Produce internally:

```text
CAPABILITY
OWNER
STRUCTURAL STATUS
RUNTIME STATUS
ARTIFACT STATUS
PRODUCTION STATUS
EVIDENCE
BLOCKER
```

---

# 6. PHASE B — REGRESSION GATE

Before production changes:

run the complete current regression suite.

Record exact:

tests
passes
failures
errors
skips

Also run:

* compile;
* Skill validation;
* documentation validation;
* package integrity;
* external-CWD smoke test;
* import DAG/cycle validation;
* secret/path/weight scans where applicable.

If baseline regression fails:

fix that before production work.

---

# 7. PHASE C — RUNTIME DISCOVERY

Discover the actual current ComfyUI environment.

Record:

runtime_version
node_inventory
node_inventory_hash
selected_device
device_free_vram
device_total_vram
queue_state
workflow_hash
workflow_fingerprint
model_hash
critical_custom_nodes
resolution
frame_count
resource_guard

Do not rely on historical runtime evidence after material changes.

---

# 8. RESOURCE SAFETY

The project already encountered OOM.

Preserve and strengthen fail-closed resource behavior.

Before local generation:

selected device MUST be known.

free VRAM MUST be observed.

resource floor MUST be explicit.

margin MUST be explicit.

If:

FREE_VRAM < REQUIRED_FLOOR × MARGIN

then:

BLOCK BEFORE SUBMISSION.

Do not claim the guard guarantees inference success.

It is a scheduling/resource gate.

---

# 9. DO NOT DESTROY OTHER WORKLOADS

Do not cancel unrelated ComfyUI jobs.

Do not clear user queues destructively.

Do not kill processes belonging to other work.

If runtime capacity is unavailable:

record:

BLOCKED_BY_RUNTIME_CAPACITY

and continue independent tasks.

---

# 10. LF-001 — PRIMARY RELEASE BLOCKER

Complete LF-001 before moving to complex long-form production.

LF-001 represents:

HUMAN → VEHICLE ENTRY.

Required causal chain:

APPROACH
→ PRE_CONTACT
→ HANDLE_CONTACT
→ DOOR_OPEN
→ BODY_ORIENTATION
→ ENTRY
→ SEATED_STATE
→ DOOR_CLOSE

The existing A003 S01 may be reused ONLY if its exact artifact/evidence remains valid and it satisfies the new shot contract.

Do not promote historical evidence automatically.

---

# 11. LF-001 SHOT STRATEGY

Use the smallest reliable number of shots.

Likely:

S01 — approach / pre-contact

S02 — handle / door articulation / body orientation

S03 — entry / seated result / door close

But do NOT force this decomposition if actual model behavior suggests a better split.

The causal phases must remain observable.

---

# 12. LF-001 SHOT CONTRACT

Each shot must declare BEFORE generation:

SHOT_ID
PURPOSE
DURATION
START_STATE
END_STATE
ACTIVE_SUBJECT
ACTION_PHASES
REFERENCES
CAMERA
ENVIRONMENT
LIGHTING
INTERACTION
ACCEPTANCE_CRITERIA
DEPENDENCIES

Do not write acceptance criteria after seeing the output.

---

# 13. LF-001 STATE

Track at minimum:

subject.identity
subject.wardrobe
subject.position
subject.orientation
subject.hand_occupancy
subject.seated_state

vehicle.identity
vehicle.geometry
vehicle.color
vehicle.position
vehicle.door_state
vehicle.motion_state

environment.identity
environment.light_direction

camera.screen_direction

interaction.phase

---

# 14. LF-001 SEMANTIC ACCEPTANCE

Observe at least:

IDENTITY
WARDROBE
VEHICLE_GEOMETRY
VEHICLE_COLOR
DOOR_STATE
HAND_CONTACT
BODY_COLLISION
ENTRY_CAUSALITY
SEATED_RESULT
ENVIRONMENT
LIGHTING
CAMERA_CONTINUITY
TEMPORAL_CONTINUITY

Statuses:

PASS
FAIL
PARTIAL
NOT_OBSERVED
NOT_APPLICABLE

A critical FAIL blocks LF-001 acceptance.

---

# 15. LF-001 TRANSITION ACCEPTANCE

For:

S01 → S02

and:

S02 → S03

require:

two distinct real artifacts;
artifact hashes;
observations for both;
end/start state compatibility;
identity continuity;
environment continuity;
action-phase continuity.

Reject:

same bytes reused;
missing observation;
state contradiction;
unexplained teleport;
unexplained door change;
unexplained screen-direction reversal.

---

# 16. LF-001 ASSEMBLY

If multiple clips are used:

create final assembly.

Manifest must bind:

final artifact
→ source shot IDs
→ artifact hashes
→ attempt IDs
→ transitions
→ repair lineage

Validate:

duration
fps
resolution
codec
audio
shot order
missing clips
duplicate clips
black/freeze
A/V alignment

---

# 17. REAL REPAIR LOOP

At least one genuine production failure should exercise repair if such a failure naturally occurs.

Do not intentionally sabotage a successful artifact merely to create a repair.

When failure occurs:

DETECT
→ LOCALIZE
→ OWNER
→ MINIMAL CHANGE
→ NEW ATTEMPT
→ OBSERVE
→ TRANSITION REVALIDATION
→ REASSEMBLY

Preserve original failed attempt.

---

# 18. REPAIR OWNERS

Possible owners:

REFERENCE
SHOT
CONTINUITY
INTERACTION
PERFORMANCE
CAMERA
CONSTRAINT
PROMPT
ADAPTER
RUNTIME
AUDIO
ASSEMBLY

Repair the smallest correct owner.

---

# 19. REPAIR BUDGET

Respect bounded repair.

Track:

attempts_per_shot
total_attempts
runtime
external_cost
human_checkpoint

Do not create uncontrolled generation loops.

---

# 20. RE-ANCHOR

Test real re-anchor if drift occurs.

Canonical anchor hierarchy:

ORIGINAL SUBJECT REFERENCE
+
ORIGINAL OBJECT/VEHICLE REFERENCE
+
ORIGINAL ENVIRONMENT REFERENCE
+
VALIDATED PREVIOUS BOUNDARY FRAME

Generated output is NOT automatically canonical truth.

---

# 21. RE-ANCHOR DECISION

Support:

CONTINUE
RE_ANCHOR
RESET
SPLIT
REGENERATE

Record:

drift detected
dimension
anchor selected
reason
result after repair

---

# 22. FLF VALIDATION

After LF-001 runtime is stable, validate endpoint conditioning if available.

Test independently:

FIRST_ONLY
LAST_ONLY
FIRST_AND_LAST

Observe:

start fidelity
end fidelity
identity
object
environment
motion
transition artifacts

Node existence is not evidence.

A successful graph preflight is not evidence.

Actual artifact observation is required.

---

# 23. FLF RESULT

Use:

CONFIRMED
PARTIAL
UNSUPPORTED
UNKNOWN

If FLF fails:

do not force it into long-form architecture.

Use canonical re-anchor + editorial transitions.

---

# 24. LF-001 EXIT GATE

Do not move LF-001 to ACCEPTED unless:

all required shots exist;
required dimensions observed;
transitions accepted;
assembly accepted;
no critical semantic FAIL;
provenance resolves;
editorial result appropriate to fixture.

If it remains PARTIAL:

state exactly why.

---

# 25. LF-002 — DIALOGUE / PERFORMANCE / AUDIO

Only after the core production pipeline is working reliably, execute LF-002.

Target:

20–30 seconds.

Two recurring adult characters.

Scenario must include:

QUESTION
→ LISTENING
→ PROCESSING
→ REACTION
→ RESPONSE
→ SIMPLE OBJECT INTERACTION

Avoid adding vehicle complexity here.

The purpose is dialogue.

---

# 26. DIALOGUE MODEL

Each turn must represent:

speaker
listener
line
intent
delivery
emotion
start
end
pause
overlap_policy
gaze_target
gesture
listener_reaction
reaction_order
voice_strategy
lip_sync_strategy

---

# 27. DIALOGUE CAUSALITY

Enforce:

STIMULUS
→ PROCESSING
→ REACTION
→ RESPONSE

Reject:

reaction before stimulus;
response before line completion;
speaker swap;
listener mouthing full speaker line;
unmotivated emotional jump.

---

# 28. AUDIO PIPELINE

Do not require one model to do everything.

Allow modular:

SCRIPT
→ VOICE
→ VISUAL
→ LIP_SYNC
→ FOLEY
→ AMBIENCE
→ MIX
→ ASSEMBLY

Use whichever supported local/authorized path produces the strongest evidence.

---

# 29. AUDIO LAYERS

Support where applicable:

DIALOGUE
FOLEY
AMBIENCE
ROOM_TONE
VEHICLE
ANIMAL
MUSIC
TRANSITION
NON_DIEGETIC

Do not require irrelevant layers.

---

# 30. LIP-SYNC

Classify actual path:

NATIVE_CONFIRMED
EXTERNAL_CONFIRMED
PARTIAL
FAILED
UNKNOWN

Evaluate:

active speaker
mouth-motion window
listener inactivity
gross timing
face visibility
occlusion
A/V alignment

Do not claim phoneme-perfect sync without a suitable oracle.

---

# 31. LF-002 ACCEPTANCE

Require applicable:

identity
wardrobe
speaker
listener
dialogue timing
reaction order
performance
gaze
object interaction
audio intelligibility
voice consistency
lip-sync
camera axis
environment
assembly

---

# 32. LF-003 — 45–60 SECOND PRODUCTION

Only after LF-001 and LF-002 infrastructure is usable.

Create a bounded but meaningful cinematic/commercial scene.

Required:

2 recurring subjects
dialogue
emotional progression
movement
persistent object
physical interaction
multiple camera setups
environment continuity
audio
assembly
repair path
human editorial review

---

# 33. LF-003 SCENE BIBLE

Must own:

identity anchors
wardrobe
relationships
environment
time of day
light direction
persistent objects
voice identity
visual language
camera language
prohibited drift

Do not duplicate transient shot state.

---

# 34. LF-003 TIMELINE

Create narrative timeline before prompts.

Example shape:

ESTABLISH
→ DIALOGUE
→ REACTION
→ MOVEMENT
→ INTERACTION
→ TRANSITION
→ SECOND BEAT
→ RESOLUTION
→ CLOSING SHOT

Exact story is flexible.

---

# 35. LF-003 SHOT GRAPH

Expected approximately:

6–12 shots

but use complexity rather than arbitrary count.

Each edge carries only relevant state.

---

# 36. LF-003 CONTINUITY

Track applicable:

identity
wardrobe
hair
position
pose
gaze
hand occupancy
object state
ownership
environment
lighting
screen direction
camera geography
emotion
dialogue
audio
time

---

# 37. LONG-FORM RE-ANCHOR

Do not propagate generated frames indefinitely.

Use canonical resets:

periodically
or
on detected drift.

Record every reset.

---

# 38. LF-003 TRANSITIONS

Every dependent transition requires:

real previous artifact
real next artifact
semantic observations
state comparison
continuity scorecard

No placeholder PASS.

---

# 39. LF-003 FINAL ASSEMBLY

Final artifact must be reproducibly linked to accepted shots.

Validate:

duration
shot order
transitions
fps
resolution
codec
audio
A/V sync
black/freeze
lineage

---

# 40. LF-003 EDITORIAL REVIEW

Separate human editorial review:

PACING
ACTING
CAMERA
EMOTION
FRAMING
RHYTHM

Editorial rejection does not mean runtime failure.

Runtime success does not imply editorial success.

---

# 41. LF-004

Do NOT prioritize 90–120 seconds until LF-001 through LF-003 have meaningful production evidence.

LF-004 may remain:

NOT_RUN

without harming the phase if shorter ladders are not yet mature.

---

# 42. SECOND REAL ADAPTER

After primary H3 closure, attempt one second real adapter if already available or safely accessible.

Do not download multi-GB model assets without authorization.

Candidate selection priority:

AVAILABLE
SUPPORTED
EXECUTABLE
EVIDENCE-FRIENDLY

not:

POPULAR.

---

# 43. ADAPTER DIFFERENTIAL

Use the SAME canonical simple scene.

Compile for:

Adapter A

and:

Adapter B.

Core semantics MUST remain unchanged:

identity
intent
causality
shot purpose
state
reference locks
acceptance criteria

Only model-specific expression may change.

---

# 44. SECOND ADAPTER RESULT

Use:

PROVEN
PARTIAL
BLOCKED
UNKNOWN

If unavailable:

BLOCKED is acceptable.

Do not fabricate a multi-model claim.

---

# 45. PROMPT COMPILER PRODUCTION TUNING

Use observed generation results to tune prompt compilation.

Evaluate whether prompts are:

too verbose
too redundant
too weak
contradictory
model-inappropriate

Preserve high-value semantics:

IDENTITY
CAUSALITY
CONTACT
STATE
CAMERA
REFERENCES
AUDIO INTENT

---

# 46. PROMPT DENSITY

Use risk-shaped density.

LOW RISK:
concise.

DIALOGUE:
turn/performance emphasis.

CONTACT:
interaction emphasis.

VEHICLE:
geometry/physics emphasis.

LONG FORM:
state/anchor emphasis.

Do not dump universal negative lists.

---

#
