# Forward-use revision pass

This is a revision pass in the same evaluator context, not a fresh-context independent critique. I re-read the eight requested raw scenarios and the current packaged skill/relevant references. No baseline scenario documents, network, inference runtime or media were used. Original response, review and CLI evidence files were left unchanged. Exact hashes of the inspected package texts and executed Python sources are in `forward-responses-r2.json`.

## Actual revised responses

The new JSON contains eight authored responses, not keyword scores. G-011 now has fifteen consecutive shots totaling 60 seconds. Its car sequence separately covers passenger-door opening, key retrieval and handoff, walking around the car, driver-door opening, each occupant entering, door closure, belts, ignition/gear selection and departure. Five seconds are allocated to key placement/belts/brake, instead of hiding these inside the previous overloaded entry shot. The push-button/key-fob car is explicitly proposed, so actual car geometry must be inspected before binding it. The final two seconds depict the beginning of the drive away; it does not claim the car fully exits a long driveway.

G-012 has fourteen shots totaling 90 seconds, partitioned exactly into three 30-second segments. The opening establishes the drone pad, controller and unfolded stretcher. Shot 6 explicitly lands the drone, stops its rotors and sets the controller down before D handles the stretcher. D's earlier radio report uses a declared headset while both hands remain on controls. Shot 8 moves the introduced stretcher from staging to casualty; shot 9 boards the ambulatory casualty while the stretcher is grounded; shots 10 and 13 cover lift and supported set-down. No landing or prop-introduction insertion is deferred. The safe near-bank evacuation route, casualty, headset and stretcher are disclosed creative proposals; the supplied request never specified the rescue target or method.

Adversarial routing now distinguishes the proposed planning decision from execution status:

- ADV-002: ASK which jacket color governs; retain both branches and continue independent identity planning.
- ADV-006: DEGRADED segmented-minute alternative; the original continuous execution remains blocked.
- ADV-007: SPLIT into establishing, neutral gaze and opposite-side reveal coverage; disclose that exact continuous orbit is changed.
- ADV-010: DEGRADED separate voice/sync/mux plan with concrete stage inputs and outputs; unsupported native execution stays blocked.
- ADV-013: BLOCK only unverified real-person face/voice binding; fictional planning remains available.
- ADV-016: DEGRADED scoped hints, input requirements and observable QA rather than a zero-error promise; actual scene/control inputs remain missing.

## Package changes inspected

The current core contract documents exact assembly/reset inner keys and distinguishes planning assembly from media assembly. It links canonical QG identifiers beside `acceptance_ids`. These directly address the two authoring-discoverability failures observed in the first pass. The top-level skill now states that unresolved causal prerequisites/action load preclude calling a treatment SUPPORTED_PLAN, and links decision routing to separate fallback planning from executable capability.

I did not independently rerun a deliberately malformed treatment in this pass, so I do not claim the reported prepare-failure diagnostic improvement was behaviorally verified here.

## Unchanged G-004 CLI rerun

The previously authored G-004 treatment was copied unchanged into a new temporary directory and run through the current prepare, validate and compile commands. All three exited 0; validate returned PASS with scope STRUCTURAL_PLAN_ONLY and no issues. Exact commands, outputs, temporary paths and artifact hashes are retained under `g004_cli_rerun` in the new JSON. Original failed/successful evidence was not rewritten. The treatment still has pending voice/sync inputs and no selected profile; compile success establishes neither feasibility nor lip-sync quality.

## Remaining limits

The long-form responses are inspectable planning records, not canonical ScenePlan JSON validated by the bundled CLI. I checked consecutive time coverage and exact totals; only G-004 received prepare/validate/compile. The long-form timing is an authored editorial proposal, not observed human/model performance. In particular, the two-second ignition/gear insert in G-011 and the short rescue radio turns require actual timing review, although their causes and support/occupancy states are now explicit. Actual references, licensed/approved voices, feature probes, frames, motion and final audio are absent. Runtime and media checks remain NOT_RUN; execution prerequisites remain explicitly blocked.

One wording tension remains: the general reference paragraph says to apply narrower scope after explicit user precedence, while the newer decision-routing paragraph says a supplied blue/red conflict requires a question unless precedence is already explicit. The concrete example makes this case actionable, but the general rule should eventually state when narrower scope can resolve a value and when it is only a proposal. This pass used the more specific blue/red instruction.

Verdict: the targeted planning and usability failures are materially improved in these eight revised responses. This is not an all-scenario semantic PASS, a fresh-critic verdict, or a production/media-quality claim.
