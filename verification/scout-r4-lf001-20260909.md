# R4 LF-001 scout memo

**Reviewer:** fresh read-only scout `Beauvoir` (`01a087d2-c6bb-7103-ab99-964ae91ae879`)

**Scope:** current prompt, Skill/runtime code, H3 workflow, LF-001 records and live local runtime. No files were edited and no runtime job was submitted, polled or cancelled.

## Finding

The full LF-001 closure is not immediately proven. A bounded local route exists after a fresh shot-specific profile/context binding. Existing evidence remains: S01 A003 is a real 5.1667 s, 384×224, 124-frame, 24 FPS MP4 with mechanical QA PASS but semantic/continuity PARTIAL; A001/A002 are immutable OOM failures; S02/S03, transitions, repair/re-anchor and assembly are absent.

## Runtime and binding observations

- Live ComfyUI is 0.34.0; the queue was empty; approximately 11.75 GB and 12.36 GB were free on cuda:0 and cuda:1 at the scout observation.
- The current H3 workflow preflight is structurally valid, but its graph hash/fingerprint differ from the older confirmed profile. The profile is therefore expired for this exact graph.
- The graph binds `cuda:0` in `ClipProjLoader`. A new run must bind workflow device, context selected device, profile and resource requirement consistently; selecting cuda:1 only in the resource object is invalid.
- The H3 profile is exact-workflow scoped. Changing prompt, seed, device, dimensions or graph requires a new profile revision or an explicit bounded probe.
- The H3 envelope is fixed at 124 frames / 5.1667 seconds. The LF-001 10–15 second chain must use explicit segment contracts and validated trimming/assembly rather than pretending a native segment has the requested duration.

## Required route

1. Run fresh `discover` and `preflight` against the exact graph.
2. Create an R4 probe/context with selected device, resource floor/margin, current node/model/workflow hashes and shot contract.
3. Submit at most one authorized local diagnostic probe, poll the same prompt ID, and collect without retrying a timeout.
4. If the probe succeeds, create a new scoped profile revision from observed runtime evidence; then attempt production shots with distinct immutable contexts and artifacts.
5. Run media QA, semantic observation, continuity dimensions, transitions and assembly separately. Never promote the probe merely because bytes are decodable.

## Blockers and limitations

- The existing S01 contract lacks complete explicit subjects, vehicle state, action phases, camera, lighting, interaction, constraints, acceptance and dependency fields.
- A003 has no verified observed end state, so it cannot authorize a dependent S02 transition.
- The T2V graph has no reference image input; identity and vehicle retention across shots remain unproven.
- If a POST times out, inspect the same queue/history ID and do not resubmit. Do not reuse A003's resource snapshot.
